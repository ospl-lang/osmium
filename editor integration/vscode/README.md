# ospl-textmate

Provides textmate extensions for OSPL.

## Install

Run `npx @vscode/vsce package --allow-missing-repository`
in the root folder to produce a VSIX.

Then right click the new file in VS-Code and press
"install extension VSIX."

Then run `code --install-extension ospl-textmate-0.0.1.vsix`
